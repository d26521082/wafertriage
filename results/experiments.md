# Experiment log

One line per run. Command is the full reproduction recipe; never delete rows.
All metrics are on the **validation** split — test stays locked until the final report.

| run | command | val macro-F1 | accuracy | notes |
|---|---|---|---|---|
| baseline | `uv run python src/train_baseline.py` | 0.8675 | 0.9759 | plain CE; misses concentrate on Loc/Scratch/Edge-Loc, errors flow to none (17–20%) |
| weighted_inv | `uv run python src/train_baseline.py --weights inv --run weighted_inv` | 0.8428 | 0.9596 | recalls +8–18pp on weak classes, precision collapses (Scratch 0.81→0.55); trades ~3 false alarms per extra catch — good or bad depends on cost ratio, not F1 |
| weighted_sqrt | `uv run python src/train_baseline.py --weights sqrt --run weighted_sqrt` | **0.8760** | 0.9771 | best of the three; most of inv's recall gains at a fraction of the precision cost (Scratch R 0.86 at P 0.71, Loc R 0.80 at P 0.79). **Supervised reference** — semi-supervised must beat this, not baseline |
| sqrt_aug | `uv run python src/train_baseline.py --weights sqrt --augment --run sqrt_aug` | 0.8725 | 0.9754 | dihedral augmentation ≈ wash (Donut/Near-full up, Loc/Random/Center down). Likely cause: real fabs have consistent wafer orientation, so the invariance assumption discards genuine directional signal. Reference stays weighted_sqrt; the augmentation code returns as FixMatch's weak-augment ingredient |
| semi_v1 | `uv run python src/train_semi.py --run semi_v1` | 0.8539 | 0.9735 | **pseudo-label collapse, textbook**: none share of pseudo-labels 93%→97% over 20 epochs while val macro-F1 decayed 0.854→0.601; best checkpoint = epoch 1 ≈ warm start, i.e. the semi mechanism only hurt. Design fault found: unsup CE was unweighted while sup CE was sqrt-weighted, amplifying the none flood |
| semi_v2 | `uv run python src/train_semi.py --run semi_v2 --weighted-unsup --exclude-none` | 0.8203 | 0.9532 | anti-collapse package stopped the none flood (pseudo-rate 92%→6.6%) but confirmation bias re-hosted in Edge-Loc (precision 0.52): confident-but-wrong defect pseudo-labels self-amplify. Second failure mode of the same disease |
| ae_finetune (v3) | `uv run python src/train_ae.py && uv run python src/train_baseline.py --weights sqrt --init-encoder results/ae_encoder.pt --run ae_finetune` | 0.8708 | 0.9743 | autoencoder pretraining on 638k unlabeled → fine-tune. ≈ wash vs weighted_sqrt (−0.5pp, within noise). No collapse (mechanism immune) but no gain either. Emerging conclusion: **with all 25k defect labels available, unlabeled data adds nothing at this model scale — the label-scarcity story needs an actual scarcity axis** (label-efficiency curve: subsample labels, compare supervised vs AE-init) |
| label-efficiency sweep (18 runs) | see `results/label_efficiency.txt`; driver = loop over `--label-frac {0.10,0.03,0.01} --seed {1,2,3} [--init-encoder]`, epochs 45/80/150 | — | — | **The scarcity axis answers.** Mean macro-F1 (scratch vs AE): 10% 0.787/0.779 (−0.9pp), 3% 0.687/0.701 (**+1.4pp, 3/3 seeds**), 1% 0.579/0.604 (**+2.5pp, 3/3 seeds**). Crossover ≈ 5–10k labels: **pretraining on 638k unlabeled maps pays only under true scarcity, and the payoff grows as labels shrink**; above the crossover it mildly hurts (prior mismatch). 6/6 seed-consistent wins below crossover. Figure: `results/label_efficiency.png` |
